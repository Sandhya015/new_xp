# XpertIntern Backend — Python 3.11+ (per Tech Stack Guide)
flask>=3.0.0
flask-cors>=4.0.0
flask-jwt-extended>=4.6.0
python-dotenv>=1.0.0
pymongo[srv]>=4.6.0
# bcrypt removed to avoid GLIBC native-extension issues on Lambda; we use werkzeug.security
# Lambda deployment (serverless-wsgi)
serverless-wsgi>=3.0.0
razorpay>=1.4.2
fpdf2>=2.7.0
# PDF certificates: fpdf2 (pure Python; ReportLab/Pillow breaks on Lambda slim layer)
# Email: SMTP (Zoho) or SES (boto3 — bundled on Lambda; listed for local SES tests)
boto3>=1.34.0
# Email: SMTP (Zoho)
# Excel: openpyxl
